name: api_generator
publish_to: 'none'
description: A new Flutter package project.
version: 0.0.3
homepage: null

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  api_annotations: 
    git: 
      url: https://github.com/KeithMarcusXIII/api_generator.git
      path: annotations
  built_collection: ^5.1.0
  code_builder: ^4.1.0
  dio: ^4.0.0
  flutter:
    sdk: flutter
  source_gen: ^1.0.3
  tuple: ^2.0.0
  logger: ^1.0.0
  json_serializable: ^4.1.4
  freezed_annotation: ^0.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  source_gen_test: ^1.0.1

# dependency_overrides:
#   api_annotations:
#     path: ../annotations

flutter: null
